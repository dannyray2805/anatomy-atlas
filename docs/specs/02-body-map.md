# 02 — Body map

v1: one sex (male Visible Human + matching HuBMAP male GLBs unless sources.md says otherwise).

Layers (coarse): skin | fascia | muscle | skeleton | organ | vessel | nerve.

Body context comes from NLM Visible Human downsampled slices, not from generated meshes.

Organ geometry comes from HuBMAP HRA GLBs (CC BY 4.0).

Clicking an unnamed mesh primitive must show `DATA_MISSING`. Do not guess labels from shape.
